const numsArray = [1, 2, 3]
numsArray[0] = 4
const str = "Olá"
str[0] = "U"
console.log(numsArray, str)

// Um array nada mais é que uma sequência de valores armazenados em posições em série na memória.
// Enquanto uma string é um conjunto de caracteres armazenados em um único espaço de memória.